# docker

Docker Compose files for services running in Phase 1 and Phase 2, before migration to Kubernetes.

Each service has its own subdirectory with a `docker-compose.yml` and a README explaining what it runs and why.

_Content added during Phase 1._
